import { EventEmitter, TemplateRef, OnInit, OnChanges, SimpleChanges, OnDestroy } from '@angular/core';
import { ColDef, DomLayoutType, GridApi, GridReadyEvent } from 'ag-grid-community';
import { HttpClient } from '@angular/common/http';
import { DialogboxService } from '../dialogbox/dialogbox.component';
import { ApiConfigService } from '../../services/api-config.service';
import { ExtendedGridOptions, GridApiConfig } from '../../interfaces/grid.interface';
import * as i0 from "@angular/core";
/**
 * Grid Component with Menu Support
 *
 * This component extends the standard ag-grid with menu functionality.
 *
 * Menu Configuration:
 * 1. Set menuActions in gridOptions to automatically add a menu column
 * 2. Or manually add a column with cellRenderer: 'gridMenu'
 *
 * Example usage:
 * ```typescript
 * this.gridOptions = {
 *   menuActions: [
 *     {
 *       title: 'View',
 *       icon: 'visibility',
 *       click: (param) => this.onView(param.data)
 *     },
 *     {
 *       title: 'Edit',
 *       icon: 'edit',
 *       click: (param) => this.onEdit(param.data)
 *     },
 *     {
 *       title: 'Delete',
 *       icon: 'delete',
 *       click: (param) => this.onDelete(param.data)
 *     }
 *   ]
 * };
 * ```
 */
export declare class GridComponent implements OnInit, OnChanges, OnDestroy {
    private dialogbox;
    private platformId;
    private apiConfigService;
    private http;
    rowData: any[];
    columnDefs: ColDef[];
    gridOptions: ExtendedGridOptions;
    domLayout: DomLayoutType;
    animateRows: boolean;
    class: string;
    height: string;
    width: string;
    showHeader: boolean;
    searchPlaceholder: string;
    rightTooltemplateRef: TemplateRef<any> | null;
    cardTemplate: TemplateRef<any> | null;
    defaultViewMode: 'list' | 'card';
    showAddButton: boolean;
    apiConfig: GridApiConfig | null;
    searchChange: EventEmitter<string>;
    rowClicked: EventEmitter<any>;
    addClick: EventEmitter<void>;
    searchText: string;
    filteredRowData: any[];
    isBrowser: boolean;
    processedColumnDefs: ColDef[];
    activeFiltersCount: number;
    activeFilterChips: Array<{
        label: string;
        value: string;
        field: string;
    }>;
    viewMode: 'list' | 'card';
    displayedRows: number;
    totalRows: number;
    gridApi: GridApi;
    private dataFetchSub?;
    constructor(dialogbox: DialogboxService, platformId: Object, apiConfigService: ApiConfigService, http: HttpClient);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onGridReady(params: GridReadyEvent): void;
    onModelUpdated(event: any): void;
    private updatePaginationInfo;
    onAddClick(): void;
    onColumnSettingsClick(): void;
    /**
     * Get status class based on status value
     * Helper method to automatically determine status badge class from enum/string values
     */
    static getStatusClass(statusValue: string | null | undefined): string;
    /**
     * Process column definitions to add menu column if needed
     *
     * This method:
     * 1. Checks if any column has cellRenderer: 'gridMenu'
     * 2. If menuActions exist in gridOptions but no menu column is configured, adds one automatically
     * 3. If a menu column exists, updates it with the proper cell renderer
     * 4. Auto-detects status columns and applies appropriate cell classes
     */
    private processColumnDefs;
    onSearchChange(value: string): void;
    private applySearchTo;
    onFilterClick(): void;
    private buildFilterDialogData;
    private detectInputType;
    private looksLikeDate;
    private getUniqueValues;
    private defaultOperatorsByType;
    private _activeFilters;
    private _activeLogic;
    private updateFilterChips;
    removeFilterChip(field: string): void;
    private applyAdvancedFilters;
    private evaluateFilter;
    private toDate;
    private sameDay;
    onRefresh(): void;
    onExport(): void;
    private exportToCSV;
    private downloadCSV;
    switchToListView(): void;
    switchToCardView(): void;
    toggleViewMode(): void;
    onRowClicked(event: any): void;
    ngOnDestroy(): void;
    private shouldFetchFromApi;
    private fetchGridDataFromApi;
    private sendApiRequest;
    private buildPayload;
    private buildHttpParams;
    private stripNullish;
    private appendQueryParams;
    private buildUrl;
    private extractDataArray;
    private extractCount;
    private applyFetchedData;
    private getPaginationLimits;
    static ɵfac: i0.ɵɵFactoryDeclaration<GridComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<GridComponent, "app-grid", never, { "rowData": { "alias": "rowData"; "required": false; }; "columnDefs": { "alias": "columnDefs"; "required": false; }; "gridOptions": { "alias": "gridOptions"; "required": false; }; "domLayout": { "alias": "domLayout"; "required": false; }; "animateRows": { "alias": "animateRows"; "required": false; }; "class": { "alias": "class"; "required": false; }; "height": { "alias": "height"; "required": false; }; "width": { "alias": "width"; "required": false; }; "showHeader": { "alias": "showHeader"; "required": false; }; "searchPlaceholder": { "alias": "searchPlaceholder"; "required": false; }; "rightTooltemplateRef": { "alias": "rightTooltemplateRef"; "required": false; }; "cardTemplate": { "alias": "cardTemplate"; "required": false; }; "defaultViewMode": { "alias": "defaultViewMode"; "required": false; }; "showAddButton": { "alias": "showAddButton"; "required": false; }; "apiConfig": { "alias": "apiConfig"; "required": false; }; }, { "searchChange": "searchChange"; "rowClicked": "rowClicked"; "addClick": "addClick"; }, never, never, true, never>;
}
