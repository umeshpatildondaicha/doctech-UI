import { PipeTransform } from '@angular/core';
import * as i0 from "@angular/core";
export interface TooltipConfig {
    text?: string;
    position?: 'top' | 'bottom' | 'left' | 'right';
    delay?: number;
    showArrow?: boolean;
    customClass?: string;
    maxWidth?: number;
    html?: boolean;
}
export declare class TooltipPipe implements PipeTransform {
    transform(value: any, config?: TooltipConfig | string): string;
    private formatTooltip;
    private generateDefaultTooltip;
    private formatObjectTooltip;
    private formatStringTooltip;
    private formatNumberTooltip;
    private formatBooleanTooltip;
    private generateTooltipAttributes;
    static createStatusTooltip(status: string, additionalInfo?: string): string;
    static createDateTooltip(date: Date | string, format?: string): string;
    static createProgressTooltip(current: number, total: number, unit?: string): string;
    static createUserTooltip(user: any): string;
    static ɵfac: i0.ɵɵFactoryDeclaration<TooltipPipe, never>;
    static ɵpipe: i0.ɵɵPipeDeclaration<TooltipPipe, "tooltip", true>;
}
