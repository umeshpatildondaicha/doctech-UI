import { Observable } from 'rxjs';
import { LoginRequest, UserInfo, CurrentUser, AuthState, UserType } from '../interfaces/auth.interface';
import * as i0 from "@angular/core";
/**
 * Service responsible for handling all authentication-related operations
 */
export declare class AuthService {
    private platformId;
    private readonly http;
    private readonly router;
    private readonly apiConfig;
    private readonly _authState;
    readonly authState$: Observable<AuthState>;
    readonly currentUser$: Observable<CurrentUser | null>;
    readonly isAuthenticated$: Observable<boolean>;
    readonly isLoading$: Observable<boolean>;
    readonly error$: Observable<string | null>;
    private readonly TOKEN_KEY;
    private readonly REFRESH_TOKEN_KEY;
    private readonly USER_KEY;
    private readonly REMEMBER_ME_KEY;
    constructor(platformId: Object);
    /**
     * Initialize authentication state from stored data
     */
    private initializeAuthState;
    /**
     * Authenticate user with email and password
     */
    login(loginRequest: LoginRequest, rememberMe?: boolean): Observable<boolean>;
    /**
     * Handle successful login response
     */
    private handleSuccessfulLogin;
    /**
     * Handle login errors and return appropriate error messages
     */
    private handleLoginError;
    /**
     * Handle mock login when backend is unavailable
     */
    private handleMockLogin;
    /**
     * Navigate user to appropriate dashboard after successful login
     */
    private navigateAfterLogin;
    /**
     * Logout user and clear all authentication data
     */
    logout(): void;
    /**
     * Refresh authentication token
     */
    refreshToken(): Observable<boolean>;
    /**
     * Check if current token is valid
     */
    isTokenValid(token?: string): boolean;
    /**
     * Get token expiration time
     */
    private getTokenExpiration;
    /**
     * Get current user information
     */
    getCurrentUser(): UserInfo | null;
    /**
     * Get current user type
     */
    getUserType(): string | null;
    /**
     * Check if user has specific permission
     */
    hasPermission(permission: string): boolean;
    /**
     * Check if user is of specific type
     */
    isUserType(userType: UserType): boolean;
    /**
     * Check if user is hospital admin
     */
    isHospitalAdmin(): boolean;
    /**
     * Check if user is doctor
     */
    isDoctor(): boolean;
    /**
     * Check if user is patient
     */
    isPatient(): boolean;
    /**
     * Update authentication state
     */
    private updateAuthState;
    /**
     * Store authentication data in storage
     */
    private storeAuthData;
    /**
     * Clear all stored authentication data
     */
    private clearStoredAuthData;
    /**
     * Get stored authentication token
     */
    getStoredToken(): string | null;
    /**
     * Get stored refresh token
     */
    getStoredRefreshToken(): string | null;
    /**
     * Get stored user data
     */
    getStoredUser(): UserInfo | null;
    /**
     * Get remember me status
     */
    private getRememberMeStatus;
    /**
     * Check if user is currently authenticated (synchronous method for guards)
     */
    isAuthenticated(): boolean;
    /**
     * Get authentication headers for HTTP requests
     */
    getAuthHeaders(): {
        [key: string]: string;
    };
    static ɵfac: i0.ɵɵFactoryDeclaration<AuthService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<AuthService>;
}
