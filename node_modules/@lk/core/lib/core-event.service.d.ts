import { Signal, Injector } from '@angular/core';
import { BreadcrumbItem } from './interfaces/breadcrumb.interface';
import * as i0 from "@angular/core";
/**
 * Core Event Service using Angular Signals for cross-library communication
 * This service provides a centralized event bus for the application
 */
export declare class CoreEventService {
    private injector;
    constructor(injector: Injector);
    private _menu;
    private _authState;
    private _breadcrumb;
    private _notification;
    readonly menu: Signal<string | null>;
    readonly authState: Signal<{
        userId: string;
        userType?: string;
        isAuthenticated: boolean;
    } | null>;
    readonly breadcrumb: Signal<BreadcrumbItem[] | null>;
    readonly notification: Signal<{
        message: string;
        type?: 'success' | 'error' | 'info' | 'warning';
    } | null>;
    setMenu(menu: string | null): void;
    clearMenu(): void;
    setAuth(state: {
        userId: string;
        userType?: string;
        isAuthenticated: boolean;
    } | null): void;
    clearAuth(): void;
    setBreadcrumb(breadcrumb: BreadcrumbItem[] | BreadcrumbItem | null): void;
    clearBreadcrumb(): void;
    setNotification(notification: {
        message: string;
        type?: 'success' | 'error' | 'info' | 'warning';
    } | null): void;
    clearNotification(): void;
    onMenuChange(cb: (menu: string | null) => void): () => void;
    onAuthChange(cb: (auth: {
        userId: string;
        userType?: string;
        isAuthenticated: boolean;
    } | null) => void): () => void;
    onBreadcrumbChange(cb: (breadcrumb: BreadcrumbItem[] | null) => void): () => void;
    onNotificationChange(cb: (notification: {
        message: string;
        type?: 'success' | 'error' | 'info' | 'warning';
    } | null) => void): () => void;
    static ɵfac: i0.ɵɵFactoryDeclaration<CoreEventService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<CoreEventService>;
}
