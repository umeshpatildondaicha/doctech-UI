import { EventEmitter, OnInit, OnDestroy } from '@angular/core';
import { Router } from '@angular/router';
import { SidebarMenuItem } from '@lk/core/lib/interfaces/sidebarmenu.interface';
import { MenuService } from '@lk/core';
import { AuthService } from '@lk/core';
import * as i0 from "@angular/core";
export declare class SidebarComponent implements OnInit, OnDestroy {
    private router;
    private menuService;
    private authService;
    private platformId;
    collapsed: boolean;
    toggle: EventEmitter<void>;
    iconSizeOnCollapsed: number;
    iconSizeOnExpanded: number;
    menuList: SidebarMenuItem[];
    private subscriptions;
    isMobile: boolean;
    isTablet: boolean;
    screenWidth: number;
    constructor(router: Router, menuService: MenuService, authService: AuthService, platformId: Object);
    ngOnInit(): void;
    ngOnDestroy(): void;
    isActive(path: string): boolean;
    getMenuItemsBySection(section: string): SidebarMenuItem[];
    getSectionTitle(section: string): string;
    onResize(event: any): void;
    private checkScreenSize;
    getSidebarClasses(): string;
    navigateTo(path: string): void;
    onBackdropClick(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SidebarComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SidebarComponent, "app-sidebar", never, { "collapsed": { "alias": "collapsed"; "required": false; }; }, { "toggle": "toggle"; }, never, never, true, never>;
}
